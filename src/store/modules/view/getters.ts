/**
 * Значения отображения времени работы устройства
 * @param {any} state
 */
export const worktime = (state: any) => state.worktime;

/**
 * Значения отображения напряжения бортовой сети
 * @param {any} state
 */
export const voltmeter = (state: any) => state.voltmeter;

/**
 * Значения отображения автомобиля
 * @param {any} state
 */
export const mazda = (state: any) => state.mazda;

/**
 * Значения отображения Bose
 * @param {any} state
 */
export const bose = (state: any) => state.bose;

/**
 * Значения отображения климата
 * @param {any} state
 */
export const climate = (state: any) => state.climate;

/**
 * Значения отображения дверей
 * @param {any} state
 */
export const doors = (state: any) => state.doors;

/**
 * Значения отображения ДВС
 * @param {any} state
 */
export const engine = (state: any) => state.engine;

/**
 * Значения отображения расхода
 * @param {any} state
 */
export const fuel = (state: any) => state.fuel;

/**
 * Значения отображения движения
 * @param {any} state
 */
export const movement = (state: any) => state.movement;

/**
 * Значения отображения датчиков
 * @param {any} state
 */
export const sensors = (state: any) => state.sensors;

/**
 * Значения отображения температуры
 * @param {any} state
 */
export const temperature = (state: any) => state.temperature;

/**
 * Значения отображения уровня звука
 * @param {any} state
 */
export const volume = (state: any) => state.volume;
